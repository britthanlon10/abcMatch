const OPERATION = {
    Next : "next",
    Back : "back",
    Skip : "skip",
    Pause : "pause",
    Help : "help"
};

next.addEventListener ("next", function (e) {
    console.log ("clicked");
});

